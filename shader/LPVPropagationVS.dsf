//STAGE:vs
#include "shared/GI.h"
cbuffer cb_InjectVertics
{
	float4 vertices[3];
}

float4 main(
	uint vertex_id : SV_VertexID,
	uint instance_id : SV_InstanceID) : SV_Position
{
	float4 pos = vertices[vertex_id];
	pos.z = instance_id * LPV_DIM_INVERSE;
	return pos;
}